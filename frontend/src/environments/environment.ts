/**
 * Configuration d'environnement (développement).
 * apiBaseUrl pointe vers l'API Express locale.
 */
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:3000/api',
};
